# calculator-logic
This C++ project implements a simple console-based calculator using an object-oriented design. The class provides a method to perform the requested operation and return the result. This project demonstrates basic class design, encapsulation, and control flow while keeping the calculation logic self-contained and reusable.
